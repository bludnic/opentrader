{
  "name": "@bifrost/prisma",
  "version": "0.0.1",
  "description": "",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc",
    "start": "ts-node app.ts",
    "dev": "nodemon",
    "prisma:schema:migrate": "prisma migrate dev",
    "bootstrap": "prisma generate && pnpm run build && cp -r prisma/client dist/client"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^20.5.6",
    "nodemon": "^3.0.1",
    "prisma": "^5.2.0",
    "ts-node": "^10.9.1",
    "typescript": "^4.9.5",
    "zod-prisma-types": "^2.7.9"
  },
  "dependencies": {
    "@bifrost/types": "workspace:*",
    "@prisma/client": "5.2.0",
    "zod": "3.21.1"
  }
}
