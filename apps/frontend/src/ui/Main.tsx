"use client";

import { styled } from "@mui/joy/styles";
import { APP_DRAWER_WIDTH } from "src/ui/navigation/AppDrawer";

export const Main = styled("main")({
  marginLeft: APP_DRAWER_WIDTH,
  padding: 32,
});
