"use client";

import { styled } from "@mui/material/styles";
import { APP_DRAWER_WIDTH } from "src/components/joy/ui/AppDrawer";

export const Main = styled("main")({
  marginLeft: APP_DRAWER_WIDTH,
  padding: 32,
});
